DELIVERY POLICY (CANON)

Repo contains:
- code, validators, specs, scripts (reproducible recipe)

Repo must NOT contain:
- delivery packs, parquet files, SHA256SUMS, or zip artifacts

Delivery pack is produced locally and distributed as a zip:
- includes parquet + SHA256SUMS + verify script + manifest + specs
